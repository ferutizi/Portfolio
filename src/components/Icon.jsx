const Icon = ({src, alt}) => {

    return(
        <>
            <img
                className='proyect__icon'
                src={src}
                alt={alt}
            />
            <p></p>
        </>
    )
}

export default Icon;